uname -a

根据进程名查端口号
ps -ef|grep redis

根据pid查进程
1. cat /proc/$PID/cmdline
2. ll /proc/$PID


grep -n string file_name

nl file_name | set -n  '/pass/p'

less string  ####n下一个；N上一个

cp /local/ /newlocal/
